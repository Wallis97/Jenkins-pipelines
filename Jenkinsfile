pipeline {
    agent any

    stages {
        stage('Linting') {
            steps {
                // figure out how to use pylint here xd
                echo "info: Linting code (this is a placeholder for an actual linting)"
            }
        }
    }
}